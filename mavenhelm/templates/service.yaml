apiVersion: v1
kind: Service
metadata:
  name: {{ include "mavenhelm.fullname" . }}
  labels:
    {{- include "mavenhelm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mavenhelm.selectorLabels" . | nindent 4 }}
